mutation VerifyReferralCode($code: String!) {
    verifyReferralString(referralString: $code) {
        meta {
            status
            RequestId
            ResponseCode
            ResponseMessage
        }
        affectedRows {
            uid
            username
            slug
            img
        }
    }
}