query SearchTags($tagName: String!, $offset: Int, $limit: Int) {
	searchTags(
        tagName: $tagName
        offset: $offset
        limit: $limit
    ) {
        meta {
            status
            RequestId
            ResponseCode
            ResponseMessage
        }
        counter
        affectedRows {
            name
        }
    }
}