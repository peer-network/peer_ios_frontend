mutation UpdateChat($chatid: String!, $name: String, $image: String) {
    updateChat(
        input: { chatid: $chatid, name: $name, image: $image }
    ) {
        status
        ResponseCode
        affectedRows {
            image
            name
            chatid
        }
    }
}