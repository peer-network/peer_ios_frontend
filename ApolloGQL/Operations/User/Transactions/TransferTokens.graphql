mutation TransferTokens($recipient: ID!, $numberoftokens: Decimal!, $message: String) {
    resolveTransferV2(
        recipient: $recipient
        numberoftokens: $numberoftokens
        message: $message
    ) {
        meta {
            status
            RequestId
            ResponseCode
            ResponseMessage
        }
    }
}