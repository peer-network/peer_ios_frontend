mutation TransferTokens($recipient: ID!, $numberoftokens: Int!) {
    resolveTransfer(
        recipient: $recipient
        numberoftokens: $numberoftokens
    ) {
        status
        ResponseCode
    }
}