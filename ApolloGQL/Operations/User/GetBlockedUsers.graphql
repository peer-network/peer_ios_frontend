query GetBlockedUsers($offset: Int, $limit: Int) {
    listBlockedUsers(
        offset: $offset
        limit: $limit
    ) {
        status
        ResponseCode
        affectedRows {
            iBlocked {
                userid
                img
                username
                slug
                visibilityStatus
            }
        }
    }
}