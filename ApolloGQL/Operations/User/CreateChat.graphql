mutation CreateChat($name: String!, $image: String!, $recipients: [String!]!) {
    createChat(
        input: { name: $name, image: $image, recipients: $recipients }
    ) {
        status
        ResponseCode
        affectedRows {
            image
            name
            chatid
        }
    }
}