query GetPostComments($postid: ID!, $commentLimit: Int, $commentOffset: Int) {
    listPosts(postid: $postid, commentLimit: $commentLimit, commentOffset: $commentOffset, contentFilterBy: MYGRANDMALIKES) {
        status
        ResponseCode
        affectedRows {
            amountcomments
            comments {
                commentid
                userid
                postid
                parentid
                content
                amountlikes
                amountreplies
                isliked
                createdat
                user {
                    id
                    username
                    slug
                    img
                    isfollowed
                    isfollowing
                }
            }
        }
    }
}
