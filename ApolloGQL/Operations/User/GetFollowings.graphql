query GetFollowings($userid: ID, $offset: Int, $limit: Int) {
	follows(
        userid: $userid
        offset: $offset
        limit: $limit
    ) {
        status
        ResponseCode
        affectedRows {
            following {
                id
                username
                slug
                img
                isfollowed
                isfollowing
            }
        }
    }
}