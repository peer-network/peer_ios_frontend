mutation UpdateName($username: String!, $password: String!) {
    updateUsername(username: $username, password: $password) {
        status
        RequestId
        ResponseCode
        ResponseMessage
    }
}