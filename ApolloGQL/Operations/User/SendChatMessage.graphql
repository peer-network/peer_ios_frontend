mutation SendChatMessage($chatid: ID!, $content: String!) {
    sendChatMessage(
        chatid: $chatid
        content: $content
    ) {
         status
        counter
        ResponseCode
        affectedRows {
            messid
            chatid
            userid
            content
            createdat
        }
    }
}