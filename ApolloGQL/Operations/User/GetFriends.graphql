query GetFriends($offset: Int, $limit: Int) {
	listFriends(
        contentFilterBy: MYGRANDMALIKES
        offset: $offset
        limit: $limit
    ) {
        status
        ResponseCode
        affectedRows {
            userid
            img
            username
            slug
        }
    }
}