query GetFriends($offset: Int, $limit: Int) {
	listFriends(
        offset: $offset
        limit: $limit
    ) {
        status
        ResponseCode
        affectedRows {
            userid
            img
            username
            slug
        }
    }
}