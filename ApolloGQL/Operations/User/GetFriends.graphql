query GetFriends($offset: Int, $limit: Int) {
	friends(
        offset: $offset
        limit: $limit
    ) {
        status
        ResponseCode
        affectedRows {
            userid
            img
            username
            slug
        }
    }
}