mutation FollowUser($userid: ID!) {
    toggleUserFollowStatus(userid: $userid) {
        meta {
            status
            RequestId
            ResponseCode
            ResponseMessage
        }
        isfollowing
    }
}