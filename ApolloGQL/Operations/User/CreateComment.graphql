mutation CreateComment($postid: ID!, $parentid: ID, $content: String!) {
    createComment(
        action: COMMENT
        postid: $postid
        parentid: $parentid
        content: $content
    ) {
        status
        ResponseCode
        affectedRows {
            commentid
            userid
            postid
            parentid
            content
            createdat
            visibilityStatus
            hasActiveReports
            amountlikes
            amountreplies
            amountreports
            isliked
            user {
                id
                username
                slug
                img
                visibilityStatus
                hasActiveReports
                isfollowed
                isfollowing
                isfriend
            }
        }
    }
}