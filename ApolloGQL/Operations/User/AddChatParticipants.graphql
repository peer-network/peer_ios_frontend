mutation AddParticipants($chatid: String!, $recipients: [String!]!) {
    addParticipants(
        input: { chatid: $chatid, recipients: $recipients }
    ) {
        status
        ResponseCode
        affectedRows {
            image
            name
            chatid
        }
    }
}